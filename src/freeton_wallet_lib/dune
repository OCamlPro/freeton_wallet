; generated by drom from package skeleton 'library'

(library
  (name freeton_wallet_lib)
  (public_name freeton_wallet_lib)
  (wrapped true)
  (libraries lwt_ssl freeton_ocaml_sdk ezjsonm ez_subst ez_file ez_config ez_cmdliner calendar   (select commandCrawler.ml from
  (freeton_crawler_lib -> commandCrawler.present.ml)
  (-> commandCrawler.absent.ml)
  )
)
  (preprocess (pps ppx_deriving_encoding))
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package freeton_wallet_lib))

(rule
  (targets files.ml)
  (deps
   (:data
     (source_tree files)))
  (action
      (run %{bin:ocaml-crunch} -m plain -o files.ml files/))
)

