; generated by drom from package skeleton 'library'

(library
  (name freetonSolidity)
  (public_name freeton_solidity_lib)
  (wrapped true)
  (libraries solidity-typechecker solidity-parser ocplib_stuff ez_file )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package freeton_solidity_lib))


