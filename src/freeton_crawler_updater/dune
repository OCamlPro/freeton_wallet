; generated by drom from package skeleton 'driver'
(executable
  (name main)
  (public_name freeton_crawler_updater)
  (package freeton_crawler_updater)
  (libraries freeton_crawler_db_versions )
  
  (optional)
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package freeton_crawler_updater))


