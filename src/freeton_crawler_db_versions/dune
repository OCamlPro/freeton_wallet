; generated by drom from package skeleton 'library'

(library
  (name freeton_crawler_db_versions)
  (public_name freeton_crawler_db_versions)
  (wrapped true)
  (libraries ez_pgocaml )
  
  (optional)
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package freeton_crawler_db_versions))


